import styled from 'styled-components';
import { palette } from '../styles/palette';

export const Container = styled.div`
    background-image: linear-gradient(${palette.LIGHT_BLUE}, ${palette.DARK_BLUE});
    overflow: hidden;
`;